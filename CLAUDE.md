# Memory

- This project is Peter's "Kanban board" — a local Kanban board app at kanban.html
- Tasks are stored in browser localStorage under the key `kanban-tasks`
- The Chrome extension cannot run JavaScript on file:// URLs, so task edits require modifying kanban.html directly
